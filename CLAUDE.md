# Claude Code orientation

Project orientation lives in `AGENTS.md` (industry-standard filename;
OpenCode reads it natively, Claude Code imports it via the `@` syntax
below). Keep all project guidance in that one file so both tools stay
in sync.

@AGENTS.md
